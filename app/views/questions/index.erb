
<%= link_to "New question", new_question_path  %>

<% @questions.each do | question| %>
  <p><%= question.title %></p>
  <%= link_to "Edit", edit_question_path(question) %>
  <%= link_to "Delete", question_path(question), data: {method: :delete, confirm: "Are you sure?"} %>
<% end %>